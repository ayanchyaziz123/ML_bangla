export const interviewEn = [
  {
    slug: 'interview-1-100',
    title: '100 Most-Asked ML Interview Questions',
    description: 'The top 100 ML interview questions from Google, Meta, Amazon, and Microsoft — covering theory, deep learning, statistics, coding, and system design with concise answers',
    category: 'ML Interview Questions',
    content: `
<h3>ML Fundamentals (Q1–Q25)</h3>
<p><strong>Q1. What is the bias-variance tradeoff?</strong><br/>Bias = error from wrong model assumptions (underfitting). Variance = sensitivity to data noise (overfitting). Total Error = Bias² + Variance + Irreducible Noise. Complex models → low bias, high variance.</p>
<p><strong>Q2. How do you prevent overfitting?</strong><br/>Regularization (L1/L2), Dropout, Early stopping, Data augmentation, Cross-validation, more training data, simpler architecture.</p>
<p><strong>Q3. L1 vs L2 regularization?</strong><br/>L1 (Lasso): |w| penalty → sparse weights, performs feature selection. L2 (Ridge): w² → shrinks weights but rarely zero. Elastic Net = L1 + L2.</p>
<p><strong>Q4. What is cross-validation and why use it?</strong><br/>Split data into k folds, train k times, each fold used as validation once. Provides unbiased performance estimate. Prevents overfitting to a single validation set. k=5 or 10 is standard.</p>
<p><strong>Q5. Precision vs Recall vs F1?</strong><br/>Precision = TP/(TP+FP). Recall = TP/(TP+FN). F1 = 2PR/(P+R). High recall critical for medical diagnosis (minimize missed cases). High precision critical for spam (minimize false alarms).</p>
<p><strong>Q6. How do you handle class imbalance?</strong><br/>SMOTE oversampling, undersampling, class_weight='balanced', threshold tuning, use PR-AUC instead of ROC-AUC, treat as anomaly detection.</p>
<p><strong>Q7. Why is feature scaling important?</strong><br/>Distance-based (KNN, SVM) and gradient descent algorithms are scale-sensitive. Tree-based methods (RF, XGBoost) don't need scaling. Use StandardScaler or MinMaxScaler.</p>
<p><strong>Q8. How do you handle missing values?</strong><br/>Drop rows/columns, mean/median/mode imputation, KNN imputation, model-based (IterativeImputer), add missing indicator. Tree models handle natively.</p>
<p><strong>Q9. Gradient descent variants?</strong><br/>Batch: full dataset per update (stable, slow). SGD: one sample (fast, noisy). Mini-batch: best tradeoff. Adam = momentum + adaptive learning rates per parameter.</p>
<p><strong>Q10. What is ROC-AUC?</strong><br/>Area under TPR vs FPR curve. 0.5 = random, 1.0 = perfect. Use PR-AUC for imbalanced datasets — it's more informative when positives are rare.</p>
<p><strong>Q11. Ensemble methods?</strong><br/>Bagging: parallel models + average (Random Forest). Boosting: sequential, each corrects previous errors (XGBoost). Stacking: meta-model over base model outputs. Boosting typically wins on tabular data.</p>
<p><strong>Q12. Decision tree splitting criteria?</strong><br/>Gini Impurity = 1-Σpᵢ² (default, faster). Entropy/Information Gain = -Σpᵢlog(pᵢ). MSE for regression. Both Gini and entropy give similar results in practice.</p>
<p><strong>Q13. Random Forest vs XGBoost?</strong><br/>RF: bagging + feature randomness, parallel training, robust to outliers. XGBoost: sequential boosting, built-in regularization, better performance, more hyperparameters. XGBoost often wins competitions.</p>
<p><strong>Q14. Curse of dimensionality?</strong><br/>Data becomes sparse in high dimensions, distances lose meaning, models degrade. Solve with feature selection, PCA, UMAP, or domain knowledge to reduce features.</p>
<p><strong>Q15. How does PCA work?</strong><br/>Finds eigenvectors of covariance matrix (principal components = max variance directions). Projects data to k dimensions. Choose k to retain ≥95% explained variance.</p>
<p><strong>Q16. SVM kernel trick?</strong><br/>Maps data to higher-dimensional space where linear separation is possible, without computing the mapping explicitly. RBF (Gaussian) kernel most common. C controls margin vs misclassification tradeoff.</p>
<p><strong>Q17. Why is Naive Bayes "naive"?</strong><br/>Assumes feature conditional independence given the class label — almost always false. Yet surprisingly effective for text classification, fast, works well with small data.</p>
<p><strong>Q18. Train/validation/test split best practice?</strong><br/>80/10/10 or 70/15/15. Test set used once for final evaluation only. Stratify for class imbalance. For time series, use temporal split. Never tune hyperparameters on test set.</p>
<p><strong>Q19. What is data leakage?</strong><br/>Test/future information leaking into training. Types: target leakage (future feature), train-test contamination (scaling before split). Fix: always split before any preprocessing, use Pipelines.</p>
<p><strong>Q20. Model selection metrics: AIC/BIC?</strong><br/>AIC = 2k - 2ln(L). BIC = k·ln(n) - 2ln(L). Both penalize complexity. Lower = better. BIC penalizes complex models more aggressively than AIC.</p>
<p><strong>Q21. Optimal k in k-means?</strong><br/>Elbow method (plot inertia vs k). Silhouette score (-1 to 1, higher better). Gap statistic. Domain knowledge. DBSCAN doesn't need k specified.</p>
<p><strong>Q22. Anomaly detection approaches?</strong><br/>Statistical (z-score, IQR). Distance-based (LOF). Tree-based (Isolation Forest). Autoencoder (reconstruction error). One-Class SVM. Choose based on labeled data availability.</p>
<p><strong>Q23. Feature importance methods?</strong><br/>Gini (fast, biased to high-cardinality). Permutation importance (model-agnostic, unbiased, test-set). SHAP values (gold standard, additive, consistent). LIME (local explanation).</p>
<p><strong>Q24. Hyperparameter tuning strategies?</strong><br/>Grid search (exhaustive, slow). Random search (efficient, Bergstra 2012). Bayesian optimization (smart, GP surrogate). Optuna TPE (best in practice). Random often beats grid.</p>
<p><strong>Q25. Choosing evaluation metrics?</strong><br/>Binary: AUC-ROC (balanced), F1 (imbalanced). Multi-class: macro/weighted F1. Regression: RMSE (outlier-sensitive), MAE (robust). Business metrics should always complement ML metrics.</p>

<h3>Deep Learning (Q26–Q50)</h3>
<p><strong>Q26. How does backpropagation work?</strong><br/>Forward pass: compute predictions. Backward pass: chain rule to compute gradient of loss w.r.t. each weight. Update: w -= lr × ∂L/∂w. Requires differentiable activations.</p>
<p><strong>Q27. Vanishing/exploding gradients?</strong><br/>Deep networks: gradients shrink (vanish) or grow (explode) exponentially. Solutions: ReLU, BatchNorm, residual connections, gradient clipping, LSTM gates, proper initialization.</p>
<p><strong>Q28. Activation functions comparison?</strong><br/>Sigmoid: (0,1), vanishing gradients. Tanh: (-1,1), zero-centered. ReLU: fast, dying ReLU issue. Leaky ReLU: fixes dying ReLU. GELU: smooth, used in Transformers/BERT.</p>
<p><strong>Q29. Why does Batch Normalization work?</strong><br/>Normalizes layer inputs per mini-batch. Reduces internal covariate shift. Enables higher learning rates. Slight regularization. Careful with small batches (use GroupNorm instead).</p>
<p><strong>Q30. How does Dropout work?</strong><br/>Randomly zero neurons during training (probability p). At inference: all neurons active, weights scaled by (1-p). Creates ensemble effect. Reduces overfitting. Typical p=0.2–0.5.</p>
<p><strong>Q31. CNN: convolution vs pooling?</strong><br/>Convolution: learned filters extract features (edges, textures) with weight sharing. Pooling: reduces spatial dimensions. Max pooling preserves dominant features. Stride convolution can replace pooling.</p>
<p><strong>Q32. Transfer learning?</strong><br/>Reuse pre-trained model weights. Feature extraction: freeze backbone, train head only. Fine-tuning: unfreeze all layers (needs more data). Best approach for small datasets.</p>
<p><strong>Q33. How does attention work?</strong><br/>Score = softmax(QKᵀ/√d_k). Output = Score × V. Q, K, V are linear projections of inputs. Self-attention: Q=K=V=same sequence. Multi-head: run h attention functions in parallel.</p>
<p><strong>Q34. Transformer vs LSTM?</strong><br/>LSTM: sequential, order-aware, slower training. Transformer: parallel attention, better for long-range dependencies, needs more data, dominates modern NLP.</p>
<p><strong>Q35. BERT vs GPT?</strong><br/>BERT: encoder-only, bidirectional, masked LM pretraining → classification, NER, QA. GPT: decoder-only, causal/autoregressive → text generation. T5: encoder-decoder, text-to-text.</p>
<p><strong>Q36. Why positional encoding?</strong><br/>Attention is permutation-invariant — it doesn't know token order. Positional encoding adds order information (sine/cosine or learned). RoPE (rotary) and ALiBi are modern alternatives.</p>
<p><strong>Q37. ResNet skip connections?</strong><br/>F(x) + x creates a gradient highway to earlier layers. Easier to learn residuals than full mappings. Enables 100+ layer networks. Gradient flows directly through addition.</p>
<p><strong>Q38. GAN training instability?</strong><br/>Mode collapse: generator repeats a few outputs. Vanishing gradients when discriminator wins too easily. Fix: WGAN (Wasserstein distance), spectral normalization, gradient penalty (WGAN-GP).</p>
<p><strong>Q39. Autoencoder vs VAE?</strong><br/>Autoencoder: deterministic latent code, can't generate new samples smoothly. VAE: latent = Gaussian (μ, σ), reparameterization trick, KL divergence regularizer → smooth latent space, generation possible.</p>
<p><strong>Q40. Learning rate scheduling?</strong><br/>Warmup then decay (standard for Transformers). Cosine annealing. OneCycleLR. Too high → diverge. Too low → slow. Adam is less sensitive to LR than vanilla SGD.</p>
<p><strong>Q41. Weight initialization importance?</strong><br/>All zeros → symmetry, no learning. Xavier/Glorot for tanh/sigmoid. He initialization for ReLU (variance = 2/fan_in). Bad init → vanishing/exploding gradients from step 1.</p>
<p><strong>Q42. How does LoRA work?</strong><br/>Freeze original weights W. Add low-rank update ΔW = BA where B (d×r), A (r×d), r &lt;&lt; d. Only A and B are trained. 7B model: 28GB → ~4GB with QLoRA (4-bit quantization + LoRA).</p>
<p><strong>Q43. Tokenization types?</strong><br/>Word-level: OOV problem. Character-level: long sequences. Subword (BPE, WordPiece): balance both. BPE: iteratively merge frequent pairs. GPT-4: ~100K vocabulary with BPE.</p>
<p><strong>Q44. Embeddings vs one-hot?</strong><br/>One-hot: sparse, high-dimensional, no semantic structure. Embedding: dense, low-dimensional, captures semantic similarity (king - man + woman ≈ queen). Pretrained embeddings enable transfer learning.</p>
<p><strong>Q45. Contrastive learning?</strong><br/>Pull similar pairs together, push dissimilar apart in embedding space. SimCLR: augmented views of same image = positives. NT-Xent loss. CLIP: image-text pairs. Self-supervised — no labels needed.</p>
<p><strong>Q46. Knowledge distillation?</strong><br/>Train small student to mimic large teacher's soft predictions (temperature-scaled logits). Student learns teacher's generalization. Produces smaller, faster models with near-teacher accuracy.</p>
<p><strong>Q47. Seq2Seq with attention?</strong><br/>Encoder reads input sequence → hidden states. Decoder attends to all encoder states at each step (not just final context vector). Eliminates bottleneck. Basis of Neural Machine Translation.</p>
<p><strong>Q48. Object detection approaches?</strong><br/>Two-stage (R-CNN): region proposals + classification — accurate, slow. One-stage (YOLO, SSD): direct prediction — fast, slightly less accurate. YOLO dominates real-time production use.</p>
<p><strong>Q49. Graph Neural Networks?</strong><br/>Aggregate neighbor features iteratively (message passing). GCN: spectral convolution. GraphSAGE: sample + aggregate. GAT: attention-weighted aggregation. Applications: molecules, social graphs, recommendation.</p>
<p><strong>Q50. How do diffusion models work?</strong><br/>Forward: gradually add Gaussian noise to data over T steps. Reverse: train network to predict and remove noise at each step. DDPM, DDIM, Stable Diffusion. Score matching: learn gradient of data log-density.</p>

<h3>Statistics & Math (Q51–Q65)</h3>
<p><strong>Q51. What is a p-value? Common misinterpretation?</strong><br/>P(observing data this extreme | H₀ true). NOT the probability the model is correct. p &lt; 0.05 means statistically significant, not necessarily practically meaningful.</p>
<p><strong>Q52. Confidence vs credible interval?</strong><br/>95% CI (frequentist): if experiment repeated many times, 95% of CIs contain the true parameter. 95% Credible (Bayesian): "probability the parameter is in this range is 95%." Credible interval is more intuitive.</p>
<p><strong>Q53. Central Limit Theorem in ML?</strong><br/>Sample mean of any i.i.d. distribution → Normal as n grows. Underpins hypothesis tests, confidence intervals, and convergence guarantees for mini-batch SGD.</p>
<p><strong>Q54. MLE definition?</strong><br/>argmax_{θ} P(data|θ) — find parameters making data most probable. Linear regression MSE = Gaussian MLE. Logistic regression cross-entropy = Bernoulli MLE.</p>
<p><strong>Q55. Covariance vs correlation?</strong><br/>Covariance is scale-dependent. Correlation = Cov(X,Y)/(σxσy) is scale-free, in [-1,1]. Both measure linear relationships only. Spearman correlation for monotonic non-linear relationships.</p>
<p><strong>Q56. What is entropy in information theory?</strong><br/>H(p) = -Σpᵢlog(pᵢ) — average surprise/uncertainty. Cross-entropy H(p,q) = -Σp log(q) is the classification loss. KL divergence = H(p,q) - H(p) measures distribution difference.</p>
<p><strong>Q57. Why is convexity important?</strong><br/>Convex function: local minimum = global minimum. Logistic regression is convex → guaranteed global optimum. Neural networks are non-convex but overparameterization helps SGD find good solutions.</p>
<p><strong>Q58. What is bootstrapping?</strong><br/>Resample with replacement to estimate statistic distribution. Compute confidence intervals without parametric assumptions. Powerful for small samples. Used in Random Forests (bootstrap aggregating = bagging).</p>
<p><strong>Q59. Mutual information?</strong><br/>I(X;Y) = H(X) + H(Y) - H(X,Y). Measures total dependency (linear and non-linear). Used for feature selection. Zero = independent. Unlike correlation, captures non-linear relationships.</p>
<p><strong>Q60. Eigenvalues in ML?</strong><br/>PCA: eigenvectors of covariance matrix = principal components. Eigenvalues = variance explained. Spectral clustering uses graph Laplacian eigendecomposition. Matrix stability analysis.</p>
<p><strong>Q61. What is MCMC?</strong><br/>Markov Chain Monte Carlo: sample from complex posterior distributions by constructing a Markov chain whose stationary distribution is the target. Used in Bayesian inference when conjugate priors aren't available.</p>
<p><strong>Q62. Gradient vs Hessian?</strong><br/>Gradient: first-order partial derivatives (direction of steepest ascent). Hessian: second-order (curvature). Newton's method uses Hessian inverse for faster convergence but is expensive O(n³).</p>
<p><strong>Q63. Type I vs II error tradeoff?</strong><br/>Lowering α (stricter) reduces Type I, increases Type II. Medical testing: Type II costly (miss disease → minimize FN → maximize recall). Spam filter: Type I costly (block legitimate email → maximize precision).</p>
<p><strong>Q64. What is regularization statistically?</strong><br/>L2 = MAP with Gaussian prior on weights. L1 = MAP with Laplace prior (encourages sparsity). Dropout ≈ model averaging. Early stopping ≈ implicit regularization.</p>
<p><strong>Q65. Explain the law of large numbers?</strong><br/>Sample mean converges to population mean as n → ∞. Foundation for why ML models generalize: if training set large enough, empirical risk ≈ expected risk.</p>

<h3>Coding & Data (Q66–Q80)</h3>
<p><strong>Q66. Efficient pandas for large datasets?</strong><br/>Specify dtypes on read. Use category for low-cardinality strings. Read in chunks. Use query() for filtering. Avoid apply() with lambdas — use vectorized operations. Consider Polars or Dask.</p>
<p><strong>Q67. Prevent data leakage in pipelines?</strong><br/>Always split before preprocessing. Use sklearn Pipeline — fit() only on training data, transform() on both. Especially critical for: scaling, imputation, target encoding, feature selection.</p>
<p><strong>Q68. Vectorization best practices?</strong><br/>Replace Python loops with NumPy/Pandas operations (uses SIMD CPU instructions, 10–100× faster). np.dot() not manual loops. Avoid .apply() with Python functions when vectorized alternatives exist.</p>
<p><strong>Q69. Feature engineering techniques?</strong><br/>Datetime: year, month, dayofweek, is_weekend. Numeric: log transform (skewed), binning, z-score. Categorical: one-hot, ordinal, target encoding, embeddings. Interactions: product/ratio of features.</p>
<p><strong>Q70. Time series train/test split?</strong><br/>Never random split — causes temporal leakage. Use walk-forward (expanding or sliding window) validation. Features: lag values, rolling statistics, Fourier terms for seasonality.</p>
<p><strong>Q71. Why use sklearn Pipeline?</strong><br/>Prevents leakage. Enables clean cross-validation. Simplifies GridSearchCV. Ensures training preprocessing matches inference. Models can be saved as single artifact.</p>
<p><strong>Q72. Target encoding vs one-hot?</strong><br/>One-hot explodes dimensionality for high-cardinality features. Target encoding: category → mean(target). Risk: leakage. Fix: cross-validated target encoding or Bayesian smoothing (add global mean with weight).</p>
<p><strong>Q73. Memory-efficient Python ML code?</strong><br/>Use generators for data loading. float32 instead of float64. scipy.sparse for sparse matrices. Delete unused variables + gc.collect(). Process data in batches with PyTorch DataLoader.</p>
<p><strong>Q74. SMOTE algorithm?</strong><br/>For each minority sample, find k nearest neighbors (also minority). Synthetically create new sample by interpolating between the original and a random neighbor. More effective than simple oversampling.</p>
<p><strong>Q75. Model serialization?</strong><br/>sklearn: joblib (faster than pickle for numpy arrays). PyTorch: save state_dict not full model. ONNX: framework-agnostic, cross-platform. TensorRT: GPU inference optimization for deployment.</p>
<p><strong>Q76. Confusion matrix interpretation?</strong><br/>TP (correct positives), TN (correct negatives), FP (false alarms/Type I), FN (missed/Type II). If FP costly → raise decision threshold. If FN costly (medical) → lower threshold. Always report alongside accuracy.</p>
<p><strong>Q77. EDA workflow?</strong><br/>shape/dtypes/describe(). Missing values. Distribution plots. Correlation matrix. Outlier detection. Target distribution. Feature vs target scatter plots. Class balance check. Temporal patterns if time series.</p>
<p><strong>Q78. Stratified sampling?</strong><br/>Maintain class distribution in each split. train_test_split(stratify=y). StratifiedKFold for cross-validation. Critical for imbalanced datasets — without it, splits may contain no minority class samples.</p>
<p><strong>Q79. Data versioning?</strong><br/>DVC (Data Version Control): Git for datasets. Tracks data files, reproducible pipelines. MLflow: tracks experiments including data artifacts. Delta Lake: versioned data warehouse. Essential for reproducibility.</p>
<p><strong>Q80. Efficient model inference?</strong><br/>Batch requests together. Use ONNX Runtime. Quantize to int8/float16. TensorRT for GPU. Cache embeddings. Profile with torch.profiler. Async processing. Horizontal scaling with load balancer.</p>

<h3>MLOps & System Design (Q81–Q100)</h3>
<p><strong>Q81. ML system design considerations?</strong><br/>Scale (QPS, data size), latency SLA, online vs batch inference, model update frequency, training infrastructure, monitoring plan, A/B testing strategy, failure modes, rollback plan.</p>
<p><strong>Q82. Model monitoring types?</strong><br/>Data drift (input distribution change), concept drift (input-output relationship change), performance degradation. Tools: Evidently, WhyLabs. Tests: KS test, PSI (Population Stability Index), chi-square.</p>
<p><strong>Q83. Online vs batch inference?</strong><br/>Online: real-time (fraud, recommendations), &lt;100ms latency, higher infra cost. Batch: periodic (email campaigns), higher throughput, simpler. Streaming: near real-time (Flink/Kafka), balance of both.</p>
<p><strong>Q84. Model versioning best practices?</strong><br/>Track: code (git hash), data version, hyperparameters, metrics, model artifact. Use MLflow or W&amp;B. Maintain staging → production promotion pipeline. Always keep rollback capability.</p>
<p><strong>Q85. Canary deployment?</strong><br/>Route 1-5% traffic to new model. Monitor metrics closely. Gradually ramp if healthy. Rollback immediately if regression. Blue-green: maintain two identical environments for instant switch.</p>
<p><strong>Q86. Design a recommendation system.</strong><br/>Two-stage: Two-Tower retrieval (FAISS, milliseconds, 500 candidates) → Ranking (Wide&amp;Deep/DCN, precise scores, top 10). Features: user history, item metadata, context. Evaluate: NDCG@K, Precision@K. Handle cold start with popularity/content fallback.</p>
<p><strong>Q87. CI/CD for ML?</strong><br/>Code tests (unit + integration). Data validation (Great Expectations). Model validation (RMSE threshold). Auto-deploy if all pass. GitHub Actions or Jenkins. Retrain pipeline trigger on data drift.</p>
<p><strong>Q88. Fairness in ML?</strong><br/>Demographic parity, equalized odds, individual fairness. Protected attributes: race, gender, age. Measure disparate impact. Tools: AI Fairness 360, What-If Tool. Fairness-accuracy tradeoff is real — make it explicit.</p>
<p><strong>Q89. Shadow mode testing?</strong><br/>New model receives real traffic but doesn't serve users. Compare predictions with production model offline. Test real distribution with zero user impact. Best practice before any A/B test.</p>
<p><strong>Q90. Distributed training approaches?</strong><br/>Data parallelism: same model, different data shards, gradients aggregated (AllReduce, Horovod). Model parallelism: different layers on different GPUs (large models). Pipeline parallelism: micro-batches across stages.</p>
<p><strong>Q91. Feature store purpose?</strong><br/>Centralized feature repository. Online store: low-latency serving at inference time. Offline store: batch features for training. Prevents training-serving skew. Enables feature reuse across teams. Tools: Feast, Tecton, Vertex AI Feature Store.</p>
<p><strong>Q92. Real-time fraud detection design?</strong><br/>Latency &lt;100ms. Streaming features via Flink/Kafka (transaction velocity, amount z-score, location anomaly). Model: XGBoost + neural network ensemble. Threshold tuned for business recall target. Regular retraining as fraud patterns evolve.</p>
<p><strong>Q93. A/B testing limitations?</strong><br/>Network effects (social platforms). Novelty effect (users behave differently with new features). Missing long-term effects. Multiple testing problem with many variants. Bandits (Thompson Sampling) can be more efficient.</p>
<p><strong>Q94. Multi-armed bandit vs A/B testing?</strong><br/>A/B: fixed exploration period, then exploit winner. Bandit: simultaneously explore and exploit, assigns more traffic to better-performing variants. Lower regret but harder to interpret causally.</p>
<p><strong>Q95. Model compression techniques?</strong><br/>Quantization (float32 → int8): 4× size reduction. Pruning: remove small weights. Knowledge distillation: train small model to mimic large one. Low-rank factorization (LoRA). Different tradeoffs for accuracy vs speed.</p>
<p><strong>Q96. Training-serving skew?</strong><br/>Model performs well in training but worse in production because features are computed differently. Fix: use same feature computation code for training and serving. Feature stores enforce consistency. Monitor feature distributions in production.</p>
<p><strong>Q97. Vector database use cases?</strong><br/>Semantic search, RAG retrieval, recommendation (FAISS ANN), image similarity. Tools: Pinecone, Weaviate, Qdrant, pgvector (PostgreSQL). HNSW index: fast approximate nearest neighbor. Exact vs approximate search tradeoff.</p>
<p><strong>Q98. How to handle concept drift?</strong><br/>Monitor prediction distribution and performance metrics. Triggers: statistical tests (KS), performance drop threshold. Actions: retrain on recent data, online learning, ensemble with fresh model, increase monitoring frequency.</p>
<p><strong>Q99. LLM evaluation metrics?</strong><br/>BLEU (n-gram overlap, machine translation). ROUGE (recall-oriented, summarization). Perplexity (language model quality). Human evaluation (preference, coherence). MMLU, HumanEval benchmarks. LLM-as-judge for qualitative assessment.</p>
<p><strong>Q100. Ethics and responsible ML?</strong><br/>Bias amplification from historical data. Privacy (differential privacy, federated learning). Environmental impact (large model training carbon). Transparency and right to explanation (EU AI Act). Human oversight for high-stakes decisions. Adversarial robustness.</p>
`
  },
];
